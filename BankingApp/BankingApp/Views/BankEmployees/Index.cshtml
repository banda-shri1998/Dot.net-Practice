@model IEnumerable<BankingApp.Models.BankEmployee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @if (User.IsInRole("Admin")) {
        @Html.ActionLink("Create New", "Create")
     }
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmpId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmpName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GENDER)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Branch.BranchName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.EmpId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.EmpName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.GENDER)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Branch.BranchName)
    </td>
    @*@Html.ActionLink("Edit", "Edit", new { id=item.EmpId }) |
        @Html.ActionLink("Details", "Details", new { id=item.EmpId }) |
        @Html.ActionLink("Delete", "Delete", new { id=item.EmpId })
    *@
    <td>
        @if (User.IsInRole("Admin"))
        {
            @Html.ActionLink("Delete", "Delete", new { id = item.EmpId })<text> | </text>
        }
        @if (User.IsInRole("Admin") || User.IsInRole("Employee"))
        {
            @Html.ActionLink("Details", "Details", new { id = item.EmpId }) <text> | </text>
        }
        @if (User.IsInRole("Employee"))
        {
            @Html.ActionLink("Edit", "Edit", new { id = item.EmpId }) <text> | </text>
        }

        @*@Html.ActionLink("Delete", "Delete", new { id = item.ID })*@
    </td>
</tr>
}

</table>
